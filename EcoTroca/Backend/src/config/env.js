// Importa o pacote dotenv
import dotenv from 'dotenv';

// Carrega as variáveis do arquivo .env para process.env
dotenv.config();

// Exporta apenas para garantir que este arquivo seja executado
export default {};